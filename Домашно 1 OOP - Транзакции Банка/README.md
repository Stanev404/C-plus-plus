
# Транзакции в банка

Довършете програмата, като допълните декларираните три класа, за да реализирате банкова логика.
Не може да променяте main фунцкията - единствено трябва да реализирате класовете.
* **class Account** - Представлява потребителска сметка. Тук трябва да реализирате логика по теглене и
депозиране на средства, като в нито един момент балансът не може да става отрицателен.
* **class Transaction** - Представляви превод на средства от една сметка (source) до друга сметка (target).
Транзакцията първо опитва да вземе парите и, единствено ако успее, ги добавя към целевата смекта.
Транзакцията също трябва да е защитена от повторно изпълнение.
* **class Bank** - изпълнява трансфери и пази история на успешните транзакции. При извикване на
член-функцията transfer трябва да се използва нов обект от тип транзакция, а не да се дублира
логиката. Ако въпросната създадена транзакция е успешна, тогава я добавете към списъка с успешни
транзакции, без да правите излишни копия.
